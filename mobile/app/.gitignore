
# @generated expo-cli sync-2b81b286409207a5da26e14c78851eb30d8ccbdb
# The following patterns were generated by expo-cli

expo-env.d.ts
# @end expo-cli

# Native build artifacts (generated by expo prebuild)
# EAS Build runs prebuild in the cloud, these shouldn't be committed
ios/
android/
# Signing material. `credentials.json` names the paths EAS reads; the .p12 is a
# private key and the .mobileprovision is bound to it. None of this is shareable
# and none of it belongs in a public repository.
credentials.json
*.p12
*.mobileprovision
*.p8
ios-creds/
